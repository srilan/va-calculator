
<div class="flex flex-col w-full items-center border-b-2 border-slate-500 pb-3">
  <div class="text-3xl bebas">Total Disablity Rating</div>
  <!-- Progress Bar na bilog -->
  <div class="relative w-40 h-40">
    <svg class="w-full h-full" viewBox="0 0 100 100">
      <circle
        class="text-gray-200 stroke-current"
        stroke-width="10"
        cx="50"
        cy="50"
        r="40"
        fill="transparent"
      ></circle>

      <circle
        class="text-[#b52d38]  progress-ring__circle stroke-current"
        stroke-width="10"
        stroke-linecap="round"
        cx="50"
        cy="50"
        r="40"
        fill="transparent"
        stroke-dashoffset= "<%= 'calc(400 - (' + (data.disabilityRating.disabilityRating/10) * 40 + ' * 65) / 100)'%>"
      ></circle>

        <text
          x="50"
          y="50"
          font-size="32"
          text-anchor="middle"
          alignment-baseline="middle"
          class="bebas"
        >
          <%= (data.disabilityRating.disabilityRating == 0) ? '0' : data.disabilityRating.disabilityRating+'%'  %>

        </text>

    </svg>
  </div>
  
  <div class="flex flex-col items-center mt-4">
    <% if (data.disabilityRating.calculatedRating > 0) { %>
        <div class="bebas text-2xl lg:text-lg">
          Calculated Disability Rating of 
          <span class="text-[#184997] text-2xl lg:text-xl">
            <%= String(data.disabilityRating.calculatedRating) %>%
          </span>
        </div>
    <% } %>

    <% if (data.disabilityRating.bilateralFactor) { %>
      <div class="bebas text-2xl lg:text-lg">
        Bilateral Factor of
        <span class="text-[#184997] text-2xl lg:text-xl">
          <%= String(data.disabilityRating.bilateralFactor) %>
        </span>
        was applied.
      </div>
    <% } %>
  </div>

</div>


<div class="flex flex-col items-center">
  <div class="bebas text-2xl mt-5">Total Monthly Compensation</div>
  <div class="text-2xl mont mt-2">$ <%= data.monthly.monthly %></div>
</div>
